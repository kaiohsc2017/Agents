[pytest]
; roda os testes com a raiz de import em src/, do mesmo jeito que o container
; de produção executa (WORKDIR /app/src, ver Dockerfile) — evita import
; relativo/absoluto divergente entre teste e produção.
pythonpath = src
asyncio_mode = auto
testpaths = tests
