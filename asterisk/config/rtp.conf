; =============================================================
; rtp.conf — Configuração de RTP e ICE/STUN para WebRTC
; =============================================================
; CR5 (auditoria 2026-08-20): a faixa aqui precisa bater com a faixa
; publicada no host pelo docker-compose.yml ("16501-17000:16501-17000/udp")
; — a faixa antiga (16000-16500) não tinha NENHUMA porta mapeada no host,
; tornando o RTP não roteável para fora do container. Ajustada para a faixa
; que já está de fato publicada.
[general]
rtpstart = 16501
rtpend   = 17000

; CRÍTICO para WebRTC: aceita RTP de qualquer IP após handshake ICE
strictrtp = no

; ICE — necessário para WebRTC
icesupport = yes

; STUN — descobre IP público para candidatos ICE
stunaddr = stun.l.google.com:19302

; Timeout ICE reduzido — não espera todos os candidatos
; Default é 5000ms, reduzindo para 2500ms
; (os candidatos host chegam primeiro e geralmente bastam)
iceblacklist = 0.0.0.0/8
